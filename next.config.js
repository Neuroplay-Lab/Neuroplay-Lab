/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/Neuroplay-Lab",
    reactStrictMode: true,
    images: {
        path:"/Neuroplay-Lab"
    }
};

module.exports = nextConfig;
